/* eslint-disable no-unused-vars */
import React from 'react';
import ReactDOM from 'react-dom/client'; // Importamos ReactDOM para renderizar la aplicación.
import App from './App'; // Importamos el componente principal.

const root = ReactDOM.createRoot(document.getElementById('root')); // Obtenemos el contenedor raíz del HTML.
root.render(<App />); // Renderizamos el componente `App` dentro del contenedor raíz.
